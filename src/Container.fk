#import:

%Container:
  div boder(size: 1px, type: solid, color: black, rouded: 10px)
    {content}

ContainerState:
  $name: text
  interface:
    Container(content: name)
    Container(content: name)
    section
      div
        label
        input
        div
          span

export:
  ContainerState